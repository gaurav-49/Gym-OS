# Secrets — never commit a real .env (see each backend's .env.example)
.env
.env.*
!.env.example
# ...including the per-deployment templates, e.g. deploy/.env.production.example
!.env.*.example

# Dependencies
node_modules/

# Build output
dist/
target/

# Logs & OS noise
*.log
npm-debug.log*
.DS_Store
