{
  "name": "gym-os-2-db",
  "version": "2.0.0",
  "private": true,
  "description": "GYM OS 2.0 database schema — idempotent migration for the 1.0 tables plus the 2.0 engagement/retention additions.",
  "scripts": {
    "migrate": "node migrate.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.4.5",
    "pg": "^8.11.5"
  }
}
